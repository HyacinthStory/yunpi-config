spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/HyacinthStory/yunpi-config
          search-paths: /**
          username: HyacinthStory
          password: hyacinth6012328
          default-label: ${spring.profiles.active}
          basedir: D:/installproject/Git/myrepo/yunpi-config
      label: master
    inetutils:
      ignoredInterfaces:
        - docker0
        - veth.*
      preferredNetworks:
        - 192.168

eureka:
  client:
    serviceUrl:
      defaultZone: http://root:root@127.0.0.1:8080/eureka/
  instance:
    instance-id: ${spring.application.name}:${spring.cloud.client.ipAddress}:${server.port}